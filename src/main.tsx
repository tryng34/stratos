import React from 'react'
import ReactDOM from 'react-dom/client'
import StratosLanding from './StratosLanding'
import './index.css'

ReactDOM.createRoot(document.getElementById('app')!).render(
  <React.StrictMode>
    <StratosLanding />
  </React.StrictMode>,
)
